# NextJS - App Router & Project Layout

NextJS makes it really easy to lay out pages and applications.

Not only this, it also makes it easy to choose whether code should run on the client or the server.

## App Router

Go inside your 'app' directory and add a new file called

```sh copy
contact/page.tsx
```

Inside the page.tsx file, type rfce and push enter, you should have the following:



